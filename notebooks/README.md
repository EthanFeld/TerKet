# Notebooks

- `terket_demo.ipynb`: repo walkthrough for install flow, amplitude queries, backend metadata, and benchmark usage

Open notebook from repo root after installing package and dependencies.
